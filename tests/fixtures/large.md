---
id: 01J9XQ2F8K7M3N5PLARGE
pinned: false
---

# 大きなノート

## セクション 0

朝の光が机の上に落ちて、昨日の続きを促している。

*斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。書くという行為は、考えを外に出して初めて形が定まる。

- 入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。
- 朝の光が机の上に落ちて、昨日の続きを促している。
- Rendering must stay incremental; a full rehighlight breaks the frame budget.

1. **強調**は文の途中で唐突に現れることが多い。
2. 書くという行為は、考えを外に出して初めて形が定まる。
3. 設計上の判断はあとから理由を思い出せる形で残す。

```sh
# コメント行
uv run pytest -q
```

> 往復変換を挟まない設計は、データ破損を構造的に防ぐ。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 6 | 211 |

## セクション 7

Rendering must stay incremental; a full rehighlight breaks the frame budget.

書くという行為は、考えを外に出して初めて形が定まる。往復変換を挟まない設計は、データ破損を構造的に防ぐ。

- **強調**は文の途中で唐突に現れることが多い。
- Rendering must stay incremental; a full rehighlight breaks the frame budget.
- エディタは書く速度を落としてはいけない。

1. 往復変換を挟まない設計は、データ破損を構造的に防ぐ。
2. 入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。
3. 書くという行為は、考えを外に出して初めて形が定まる。

```sh
# コメント行
uv run pytest -q
```

> `toPlainText()` の結果がそのまま保存内容になる。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 13 | 755 |

## セクション 14

往復変換を挟まない設計は、データ破損を構造的に防ぐ。

`toPlainText()` の結果がそのまま保存内容になる。タグは #work のように書き、#work/会議 で階層にする。

- 朝の光が机の上に落ちて、昨日の続きを促している。
- The quick brown fox jumps over the lazy dog near the river bank.
- **強調**は文の途中で唐突に現れることが多い。

1. `toPlainText()` の結果がそのまま保存内容になる。
2. Rendering must stay incremental; a full rehighlight breaks the frame budget.
3. 朝の光が机の上に落ちて、昨日の続きを促している。

```sh
# コメント行
uv run pytest -q
```

> エディタは書く速度を落としてはいけない。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 20 | 119 |

## セクション 21

エディタは書く速度を落としてはいけない。

設計上の判断はあとから理由を思い出せる形で残す。*斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。

- 朝の光が机の上に落ちて、昨日の続きを促している。
- 往復変換を挟まない設計は、データ破損を構造的に防ぐ。
- 書くという行為は、考えを外に出して初めて形が定まる。

1. 設計上の判断はあとから理由を思い出せる形で残す。
2. The quick brown fox jumps over the lazy dog near the river bank.
3. 朝の光が机の上に落ちて、昨日の続きを促している。

```sql
SELECT title FROM notes
WHERE trashed = 0;
```

> 入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 27 | 353 |

## セクション 28

**強調**は文の途中で唐突に現れることが多い。

往復変換を挟まない設計は、データ破損を構造的に防ぐ。`toPlainText()` の結果がそのまま保存内容になる。

- 入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。
- **強調**は文の途中で唐突に現れることが多い。
- Rendering must stay incremental; a full rehighlight breaks the frame budget.

1. エディタは書く速度を落としてはいけない。
2. **強調**は文の途中で唐突に現れることが多い。
3. The quick brown fox jumps over the lazy dog near the river bank.

```sql
SELECT title FROM notes
WHERE trashed = 0;
```

> 朝の光が机の上に落ちて、昨日の続きを促している。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 34 | 122 |

## セクション 35

入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。

書くという行為は、考えを外に出して初めて形が定まる。朝の光が机の上に落ちて、昨日の続きを促している。

- Rendering must stay incremental; a full rehighlight breaks the frame budget.
- Rendering must stay incremental; a full rehighlight breaks the frame budget.
- The quick brown fox jumps over the lazy dog near the river bank.

1. The quick brown fox jumps over the lazy dog near the river bank.
2. タグは #work のように書き、#work/会議 で階層にする。
3. 入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。

```sql
SELECT title FROM notes
WHERE trashed = 0;
```

> 往復変換を挟まない設計は、データ破損を構造的に防ぐ。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 41 | 769 |

## セクション 42

**強調**は文の途中で唐突に現れることが多い。

タグは #work のように書き、#work/会議 で階層にする。設計上の判断はあとから理由を思い出せる形で残す。

- Rendering must stay incremental; a full rehighlight breaks the frame budget.
- **強調**は文の途中で唐突に現れることが多い。
- Rendering must stay incremental; a full rehighlight breaks the frame budget.

1. Rendering must stay incremental; a full rehighlight breaks the frame budget.
2. 入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。
3. 設計上の判断はあとから理由を思い出せる形で残す。

```sh
# コメント行
uv run pytest -q
```

> **強調**は文の途中で唐突に現れることが多い。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 48 | 955 |

## セクション 49

書くという行為は、考えを外に出して初めて形が定まる。

設計上の判断はあとから理由を思い出せる形で残す。`toPlainText()` の結果がそのまま保存内容になる。

- 朝の光が机の上に落ちて、昨日の続きを促している。
- Rendering must stay incremental; a full rehighlight breaks the frame budget.
- Rendering must stay incremental; a full rehighlight breaks the frame budget.

1. 朝の光が机の上に落ちて、昨日の続きを促している。
2. Rendering must stay incremental; a full rehighlight breaks the frame budget.
3. エディタは書く速度を落としてはいけない。

```python
def scan(text: str) -> list[InlineSpan]:
    return []
```

> 朝の光が机の上に落ちて、昨日の続きを促している。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 55 | 153 |

## セクション 56

エディタは書く速度を落としてはいけない。

*斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。往復変換を挟まない設計は、データ破損を構造的に防ぐ。

- 朝の光が机の上に落ちて、昨日の続きを促している。
- タグは #work のように書き、#work/会議 で階層にする。
- 設計上の判断はあとから理由を思い出せる形で残す。

1. `toPlainText()` の結果がそのまま保存内容になる。
2. 往復変換を挟まない設計は、データ破損を構造的に防ぐ。
3. 往復変換を挟まない設計は、データ破損を構造的に防ぐ。

```sh
# コメント行
uv run pytest -q
```

> `toPlainText()` の結果がそのまま保存内容になる。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 62 | 356 |

## セクション 63

The quick brown fox jumps over the lazy dog near the river bank.

往復変換を挟まない設計は、データ破損を構造的に防ぐ。**強調**は文の途中で唐突に現れることが多い。

- タグは #work のように書き、#work/会議 で階層にする。
- Rendering must stay incremental; a full rehighlight breaks the frame budget.
- タグは #work のように書き、#work/会議 で階層にする。

1. 書くという行為は、考えを外に出して初めて形が定まる。
2. エディタは書く速度を落としてはいけない。
3. `toPlainText()` の結果がそのまま保存内容になる。

```python
def scan(text: str) -> list[InlineSpan]:
    return []
```

> 朝の光が机の上に落ちて、昨日の続きを促している。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 69 | 116 |

## セクション 70

朝の光が机の上に落ちて、昨日の続きを促している。

書くという行為は、考えを外に出して初めて形が定まる。The quick brown fox jumps over the lazy dog near the river bank.

- 設計上の判断はあとから理由を思い出せる形で残す。
- **強調**は文の途中で唐突に現れることが多い。
- 朝の光が机の上に落ちて、昨日の続きを促している。

1. 往復変換を挟まない設計は、データ破損を構造的に防ぐ。
2. *斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。
3. *斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。

```sh
# コメント行
uv run pytest -q
```

> *斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 76 | 831 |

## セクション 77

タグは #work のように書き、#work/会議 で階層にする。

**強調**は文の途中で唐突に現れることが多い。*斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。

- タグは #work のように書き、#work/会議 で階層にする。
- 入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。
- 朝の光が机の上に落ちて、昨日の続きを促している。

1. `toPlainText()` の結果がそのまま保存内容になる。
2. The quick brown fox jumps over the lazy dog near the river bank.
3. 入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。

```python
def scan(text: str) -> list[InlineSpan]:
    return []
```

> 朝の光が机の上に落ちて、昨日の続きを促している。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 83 | 329 |

## セクション 84

`toPlainText()` の結果がそのまま保存内容になる。

エディタは書く速度を落としてはいけない。書くという行為は、考えを外に出して初めて形が定まる。

- **強調**は文の途中で唐突に現れることが多い。
- *斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。
- *斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。

1. Rendering must stay incremental; a full rehighlight breaks the frame budget.
2. 設計上の判断はあとから理由を思い出せる形で残す。
3. 入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。

```python
def scan(text: str) -> list[InlineSpan]:
    return []
```

> The quick brown fox jumps over the lazy dog near the river bank.

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 90 | 687 |

## セクション 91

Rendering must stay incremental; a full rehighlight breaks the frame budget.

**強調**は文の途中で唐突に現れることが多い。The quick brown fox jumps over the lazy dog near the river bank.

- Rendering must stay incremental; a full rehighlight breaks the frame budget.
- 入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。
- Rendering must stay incremental; a full rehighlight breaks the frame budget.

1. *斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。
2. 入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。
3. 入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。

```sh
# コメント行
uv run pytest -q
```

> Rendering must stay incremental; a full rehighlight breaks the frame budget.

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 97 | 295 |

## セクション 98

The quick brown fox jumps over the lazy dog near the river bank.

入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。

- 入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。
- タグは #work のように書き、#work/会議 で階層にする。
- 朝の光が机の上に落ちて、昨日の続きを促している。

1. タグは #work のように書き、#work/会議 で階層にする。
2. 往復変換を挟まない設計は、データ破損を構造的に防ぐ。
3. `toPlainText()` の結果がそのまま保存内容になる。

```sql
SELECT title FROM notes
WHERE trashed = 0;
```

> エディタは書く速度を落としてはいけない。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 104 | 895 |

## セクション 105

The quick brown fox jumps over the lazy dog near the river bank.

書くという行為は、考えを外に出して初めて形が定まる。タグは #work のように書き、#work/会議 で階層にする。

- `toPlainText()` の結果がそのまま保存内容になる。
- エディタは書く速度を落としてはいけない。
- 入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。

1. エディタは書く速度を落としてはいけない。
2. `toPlainText()` の結果がそのまま保存内容になる。
3. The quick brown fox jumps over the lazy dog near the river bank.

```sql
SELECT title FROM notes
WHERE trashed = 0;
```

> The quick brown fox jumps over the lazy dog near the river bank.

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 111 | 835 |

## セクション 112

入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。

入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。*斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。

- 入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。
- 設計上の判断はあとから理由を思い出せる形で残す。
- 設計上の判断はあとから理由を思い出せる形で残す。

1. 設計上の判断はあとから理由を思い出せる形で残す。
2. エディタは書く速度を落としてはいけない。
3. *斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。

```python
def scan(text: str) -> list[InlineSpan]:
    return []
```

> 設計上の判断はあとから理由を思い出せる形で残す。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 118 | 53 |

## セクション 119

Rendering must stay incremental; a full rehighlight breaks the frame budget.

入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。往復変換を挟まない設計は、データ破損を構造的に防ぐ。

- 設計上の判断はあとから理由を思い出せる形で残す。
- `toPlainText()` の結果がそのまま保存内容になる。
- The quick brown fox jumps over the lazy dog near the river bank.

1. The quick brown fox jumps over the lazy dog near the river bank.
2. 設計上の判断はあとから理由を思い出せる形で残す。
3. 設計上の判断はあとから理由を思い出せる形で残す。

```sh
# コメント行
uv run pytest -q
```

> `toPlainText()` の結果がそのまま保存内容になる。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 125 | 890 |

## セクション 126

往復変換を挟まない設計は、データ破損を構造的に防ぐ。

*斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。エディタは書く速度を落としてはいけない。

- The quick brown fox jumps over the lazy dog near the river bank.
- 書くという行為は、考えを外に出して初めて形が定まる。
- *斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。

1. 設計上の判断はあとから理由を思い出せる形で残す。
2. **強調**は文の途中で唐突に現れることが多い。
3. 往復変換を挟まない設計は、データ破損を構造的に防ぐ。

```python
def scan(text: str) -> list[InlineSpan]:
    return []
```

> 往復変換を挟まない設計は、データ破損を構造的に防ぐ。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 132 | 765 |

## セクション 133

エディタは書く速度を落としてはいけない。

設計上の判断はあとから理由を思い出せる形で残す。エディタは書く速度を落としてはいけない。

- 往復変換を挟まない設計は、データ破損を構造的に防ぐ。
- エディタは書く速度を落としてはいけない。
- 入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。

1. *斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。
2. *斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。
3. 設計上の判断はあとから理由を思い出せる形で残す。

```python
def scan(text: str) -> list[InlineSpan]:
    return []
```

> Rendering must stay incremental; a full rehighlight breaks the frame budget.

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 139 | 456 |

## セクション 140

朝の光が机の上に落ちて、昨日の続きを促している。

エディタは書く速度を落としてはいけない。入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。

- 往復変換を挟まない設計は、データ破損を構造的に防ぐ。
- 往復変換を挟まない設計は、データ破損を構造的に防ぐ。
- Rendering must stay incremental; a full rehighlight breaks the frame budget.

1. `toPlainText()` の結果がそのまま保存内容になる。
2. The quick brown fox jumps over the lazy dog near the river bank.
3. 往復変換を挟まない設計は、データ破損を構造的に防ぐ。

```python
def scan(text: str) -> list[InlineSpan]:
    return []
```

> The quick brown fox jumps over the lazy dog near the river bank.

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 146 | 658 |

## セクション 147

入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。

入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。エディタは書く速度を落としてはいけない。

- **強調**は文の途中で唐突に現れることが多い。
- エディタは書く速度を落としてはいけない。
- **強調**は文の途中で唐突に現れることが多い。

1. *斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。
2. 設計上の判断はあとから理由を思い出せる形で残す。
3. 設計上の判断はあとから理由を思い出せる形で残す。

```sh
# コメント行
uv run pytest -q
```

> Rendering must stay incremental; a full rehighlight breaks the frame budget.

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 153 | 810 |

## セクション 154

入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。

`toPlainText()` の結果がそのまま保存内容になる。**強調**は文の途中で唐突に現れることが多い。

- タグは #work のように書き、#work/会議 で階層にする。
- タグは #work のように書き、#work/会議 で階層にする。
- タグは #work のように書き、#work/会議 で階層にする。

1. 朝の光が机の上に落ちて、昨日の続きを促している。
2. Rendering must stay incremental; a full rehighlight breaks the frame budget.
3. タグは #work のように書き、#work/会議 で階層にする。

```sql
SELECT title FROM notes
WHERE trashed = 0;
```

> エディタは書く速度を落としてはいけない。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 160 | 395 |

## セクション 161

Rendering must stay incremental; a full rehighlight breaks the frame budget.

設計上の判断はあとから理由を思い出せる形で残す。朝の光が机の上に落ちて、昨日の続きを促している。

- 設計上の判断はあとから理由を思い出せる形で残す。
- Rendering must stay incremental; a full rehighlight breaks the frame budget.
- Rendering must stay incremental; a full rehighlight breaks the frame budget.

1. タグは #work のように書き、#work/会議 で階層にする。
2. 入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。
3. Rendering must stay incremental; a full rehighlight breaks the frame budget.

```sql
SELECT title FROM notes
WHERE trashed = 0;
```

> **強調**は文の途中で唐突に現れることが多い。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 167 | 188 |

## セクション 168

往復変換を挟まない設計は、データ破損を構造的に防ぐ。

設計上の判断はあとから理由を思い出せる形で残す。**強調**は文の途中で唐突に現れることが多い。

- 往復変換を挟まない設計は、データ破損を構造的に防ぐ。
- エディタは書く速度を落としてはいけない。
- 朝の光が机の上に落ちて、昨日の続きを促している。

1. Rendering must stay incremental; a full rehighlight breaks the frame budget.
2. The quick brown fox jumps over the lazy dog near the river bank.
3. 往復変換を挟まない設計は、データ破損を構造的に防ぐ。

```python
def scan(text: str) -> list[InlineSpan]:
    return []
```

> Rendering must stay incremental; a full rehighlight breaks the frame budget.

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 174 | 288 |

## セクション 175

書くという行為は、考えを外に出して初めて形が定まる。

入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。往復変換を挟まない設計は、データ破損を構造的に防ぐ。

- The quick brown fox jumps over the lazy dog near the river bank.
- 書くという行為は、考えを外に出して初めて形が定まる。
- Rendering must stay incremental; a full rehighlight breaks the frame budget.

1. *斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。
2. 入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。
3. Rendering must stay incremental; a full rehighlight breaks the frame budget.

```sh
# コメント行
uv run pytest -q
```

> 朝の光が机の上に落ちて、昨日の続きを促している。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 181 | 223 |

## セクション 182

`toPlainText()` の結果がそのまま保存内容になる。

設計上の判断はあとから理由を思い出せる形で残す。朝の光が机の上に落ちて、昨日の続きを促している。

- エディタは書く速度を落としてはいけない。
- `toPlainText()` の結果がそのまま保存内容になる。
- **強調**は文の途中で唐突に現れることが多い。

1. *斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。
2. 入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。
3. タグは #work のように書き、#work/会議 で階層にする。

```python
def scan(text: str) -> list[InlineSpan]:
    return []
```

> タグは #work のように書き、#work/会議 で階層にする。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 188 | 630 |

## セクション 189

朝の光が机の上に落ちて、昨日の続きを促している。

設計上の判断はあとから理由を思い出せる形で残す。タグは #work のように書き、#work/会議 で階層にする。

- 設計上の判断はあとから理由を思い出せる形で残す。
- タグは #work のように書き、#work/会議 で階層にする。
- The quick brown fox jumps over the lazy dog near the river bank.

1. タグは #work のように書き、#work/会議 で階層にする。
2. タグは #work のように書き、#work/会議 で階層にする。
3. エディタは書く速度を落としてはいけない。

```python
def scan(text: str) -> list[InlineSpan]:
    return []
```

> タグは #work のように書き、#work/会議 で階層にする。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 195 | 16 |

## セクション 196

Rendering must stay incremental; a full rehighlight breaks the frame budget.

Rendering must stay incremental; a full rehighlight breaks the frame budget.書くという行為は、考えを外に出して初めて形が定まる。

- エディタは書く速度を落としてはいけない。
- エディタは書く速度を落としてはいけない。
- 入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。

1. 往復変換を挟まない設計は、データ破損を構造的に防ぐ。
2. 往復変換を挟まない設計は、データ破損を構造的に防ぐ。
3. 朝の光が机の上に落ちて、昨日の続きを促している。

```python
def scan(text: str) -> list[InlineSpan]:
    return []
```

> 設計上の判断はあとから理由を思い出せる形で残す。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 202 | 376 |

## セクション 203

設計上の判断はあとから理由を思い出せる形で残す。

*斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。*斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。

- *斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。
- エディタは書く速度を落としてはいけない。
- 往復変換を挟まない設計は、データ破損を構造的に防ぐ。

1. 設計上の判断はあとから理由を思い出せる形で残す。
2. エディタは書く速度を落としてはいけない。
3. Rendering must stay incremental; a full rehighlight breaks the frame budget.

```sql
SELECT title FROM notes
WHERE trashed = 0;
```

> Rendering must stay incremental; a full rehighlight breaks the frame budget.

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 209 | 3 |

## セクション 210

エディタは書く速度を落としてはいけない。

設計上の判断はあとから理由を思い出せる形で残す。`toPlainText()` の結果がそのまま保存内容になる。

- 往復変換を挟まない設計は、データ破損を構造的に防ぐ。
- エディタは書く速度を落としてはいけない。
- タグは #work のように書き、#work/会議 で階層にする。

1. 設計上の判断はあとから理由を思い出せる形で残す。
2. 往復変換を挟まない設計は、データ破損を構造的に防ぐ。
3. **強調**は文の途中で唐突に現れることが多い。

```python
def scan(text: str) -> list[InlineSpan]:
    return []
```

> *斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 216 | 673 |

## セクション 217

設計上の判断はあとから理由を思い出せる形で残す。

設計上の判断はあとから理由を思い出せる形で残す。エディタは書く速度を落としてはいけない。

- 入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。
- **強調**は文の途中で唐突に現れることが多い。
- `toPlainText()` の結果がそのまま保存内容になる。

1. 書くという行為は、考えを外に出して初めて形が定まる。
2. エディタは書く速度を落としてはいけない。
3. 往復変換を挟まない設計は、データ破損を構造的に防ぐ。

```sh
# コメント行
uv run pytest -q
```

> 往復変換を挟まない設計は、データ破損を構造的に防ぐ。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 223 | 702 |

## セクション 224

`toPlainText()` の結果がそのまま保存内容になる。

エディタは書く速度を落としてはいけない。The quick brown fox jumps over the lazy dog near the river bank.

- **強調**は文の途中で唐突に現れることが多い。
- *斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。
- 入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。

1. エディタは書く速度を落としてはいけない。
2. エディタは書く速度を落としてはいけない。
3. Rendering must stay incremental; a full rehighlight breaks the frame budget.

```python
def scan(text: str) -> list[InlineSpan]:
    return []
```

> **強調**は文の途中で唐突に現れることが多い。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 230 | 102 |

## セクション 231

入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。

朝の光が机の上に落ちて、昨日の続きを促している。The quick brown fox jumps over the lazy dog near the river bank.

- 書くという行為は、考えを外に出して初めて形が定まる。
- Rendering must stay incremental; a full rehighlight breaks the frame budget.
- 往復変換を挟まない設計は、データ破損を構造的に防ぐ。

1. **強調**は文の途中で唐突に現れることが多い。
2. 書くという行為は、考えを外に出して初めて形が定まる。
3. `toPlainText()` の結果がそのまま保存内容になる。

```sql
SELECT title FROM notes
WHERE trashed = 0;
```

> 朝の光が机の上に落ちて、昨日の続きを促している。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 237 | 737 |

## セクション 238

朝の光が机の上に落ちて、昨日の続きを促している。

*斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。朝の光が机の上に落ちて、昨日の続きを促している。

- タグは #work のように書き、#work/会議 で階層にする。
- エディタは書く速度を落としてはいけない。
- Rendering must stay incremental; a full rehighlight breaks the frame budget.

1. *斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。
2. エディタは書く速度を落としてはいけない。
3. *斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。

```sh
# コメント行
uv run pytest -q
```

> **強調**は文の途中で唐突に現れることが多い。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 244 | 384 |

## セクション 245

Rendering must stay incremental; a full rehighlight breaks the frame budget.

*斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。設計上の判断はあとから理由を思い出せる形で残す。

- 往復変換を挟まない設計は、データ破損を構造的に防ぐ。
- *斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。
- 設計上の判断はあとから理由を思い出せる形で残す。

1. 書くという行為は、考えを外に出して初めて形が定まる。
2. タグは #work のように書き、#work/会議 で階層にする。
3. 朝の光が机の上に落ちて、昨日の続きを促している。

```sh
# コメント行
uv run pytest -q
```

> 設計上の判断はあとから理由を思い出せる形で残す。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 251 | 808 |

## セクション 252

Rendering must stay incremental; a full rehighlight breaks the frame budget.

書くという行為は、考えを外に出して初めて形が定まる。*斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。

- 入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。
- `toPlainText()` の結果がそのまま保存内容になる。
- `toPlainText()` の結果がそのまま保存内容になる。

1. The quick brown fox jumps over the lazy dog near the river bank.
2. 朝の光が机の上に落ちて、昨日の続きを促している。
3. 書くという行為は、考えを外に出して初めて形が定まる。

```sh
# コメント行
uv run pytest -q
```

> タグは #work のように書き、#work/会議 で階層にする。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 258 | 501 |

## セクション 259

Rendering must stay incremental; a full rehighlight breaks the frame budget.

入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。エディタは書く速度を落としてはいけない。

- 設計上の判断はあとから理由を思い出せる形で残す。
- The quick brown fox jumps over the lazy dog near the river bank.
- 書くという行為は、考えを外に出して初めて形が定まる。

1. Rendering must stay incremental; a full rehighlight breaks the frame budget.
2. `toPlainText()` の結果がそのまま保存内容になる。
3. 朝の光が机の上に落ちて、昨日の続きを促している。

```python
def scan(text: str) -> list[InlineSpan]:
    return []
```

> *斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 265 | 237 |

## セクション 266

*斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。

書くという行為は、考えを外に出して初めて形が定まる。書くという行為は、考えを外に出して初めて形が定まる。

- 往復変換を挟まない設計は、データ破損を構造的に防ぐ。
- Rendering must stay incremental; a full rehighlight breaks the frame budget.
- `toPlainText()` の結果がそのまま保存内容になる。

1. **強調**は文の途中で唐突に現れることが多い。
2. *斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。
3. 設計上の判断はあとから理由を思い出せる形で残す。

```python
def scan(text: str) -> list[InlineSpan]:
    return []
```

> Rendering must stay incremental; a full rehighlight breaks the frame budget.

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 272 | 740 |

## セクション 273

*斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。

エディタは書く速度を落としてはいけない。設計上の判断はあとから理由を思い出せる形で残す。

- 入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。
- 設計上の判断はあとから理由を思い出せる形で残す。
- タグは #work のように書き、#work/会議 で階層にする。

1. Rendering must stay incremental; a full rehighlight breaks the frame budget.
2. 朝の光が机の上に落ちて、昨日の続きを促している。
3. 往復変換を挟まない設計は、データ破損を構造的に防ぐ。

```sh
# コメント行
uv run pytest -q
```

> *斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 279 | 344 |

## セクション 280

朝の光が机の上に落ちて、昨日の続きを促している。

`toPlainText()` の結果がそのまま保存内容になる。`toPlainText()` の結果がそのまま保存内容になる。

- 入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。
- タグは #work のように書き、#work/会議 で階層にする。
- 朝の光が机の上に落ちて、昨日の続きを促している。

1. エディタは書く速度を落としてはいけない。
2. 朝の光が机の上に落ちて、昨日の続きを促している。
3. 往復変換を挟まない設計は、データ破損を構造的に防ぐ。

```sql
SELECT title FROM notes
WHERE trashed = 0;
```

> 朝の光が机の上に落ちて、昨日の続きを促している。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 286 | 646 |

## セクション 287

朝の光が机の上に落ちて、昨日の続きを促している。

タグは #work のように書き、#work/会議 で階層にする。タグは #work のように書き、#work/会議 で階層にする。

- タグは #work のように書き、#work/会議 で階層にする。
- The quick brown fox jumps over the lazy dog near the river bank.
- 朝の光が机の上に落ちて、昨日の続きを促している。

1. タグは #work のように書き、#work/会議 で階層にする。
2. The quick brown fox jumps over the lazy dog near the river bank.
3. **強調**は文の途中で唐突に現れることが多い。

```python
def scan(text: str) -> list[InlineSpan]:
    return []
```

> Rendering must stay incremental; a full rehighlight breaks the frame budget.

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 293 | 515 |

## セクション 294

Rendering must stay incremental; a full rehighlight breaks the frame budget.

Rendering must stay incremental; a full rehighlight breaks the frame budget.書くという行為は、考えを外に出して初めて形が定まる。

- The quick brown fox jumps over the lazy dog near the river bank.
- エディタは書く速度を落としてはいけない。
- *斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。

1. The quick brown fox jumps over the lazy dog near the river bank.
2. エディタは書く速度を落としてはいけない。
3. The quick brown fox jumps over the lazy dog near the river bank.

```sh
# コメント行
uv run pytest -q
```

> 往復変換を挟まない設計は、データ破損を構造的に防ぐ。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 300 | 653 |

## セクション 301

*斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。

*斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。`toPlainText()` の結果がそのまま保存内容になる。

- タグは #work のように書き、#work/会議 で階層にする。
- エディタは書く速度を落としてはいけない。
- 入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。

1. 設計上の判断はあとから理由を思い出せる形で残す。
2. エディタは書く速度を落としてはいけない。
3. 書くという行為は、考えを外に出して初めて形が定まる。

```sql
SELECT title FROM notes
WHERE trashed = 0;
```

> 設計上の判断はあとから理由を思い出せる形で残す。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 307 | 69 |

## セクション 308

設計上の判断はあとから理由を思い出せる形で残す。

設計上の判断はあとから理由を思い出せる形で残す。書くという行為は、考えを外に出して初めて形が定まる。

- Rendering must stay incremental; a full rehighlight breaks the frame budget.
- タグは #work のように書き、#work/会議 で階層にする。
- 朝の光が机の上に落ちて、昨日の続きを促している。

1. タグは #work のように書き、#work/会議 で階層にする。
2. 入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。
3. 設計上の判断はあとから理由を思い出せる形で残す。

```python
def scan(text: str) -> list[InlineSpan]:
    return []
```

> タグは #work のように書き、#work/会議 で階層にする。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 314 | 637 |

## セクション 315

書くという行為は、考えを外に出して初めて形が定まる。

`toPlainText()` の結果がそのまま保存内容になる。往復変換を挟まない設計は、データ破損を構造的に防ぐ。

- Rendering must stay incremental; a full rehighlight breaks the frame budget.
- タグは #work のように書き、#work/会議 で階層にする。
- The quick brown fox jumps over the lazy dog near the river bank.

1. 入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。
2. **強調**は文の途中で唐突に現れることが多い。
3. エディタは書く速度を落としてはいけない。

```python
def scan(text: str) -> list[InlineSpan]:
    return []
```

> エディタは書く速度を落としてはいけない。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 321 | 537 |

## セクション 322

往復変換を挟まない設計は、データ破損を構造的に防ぐ。

書くという行為は、考えを外に出して初めて形が定まる。*斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。

- 入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。
- エディタは書く速度を落としてはいけない。
- タグは #work のように書き、#work/会議 で階層にする。

1. 往復変換を挟まない設計は、データ破損を構造的に防ぐ。
2. **強調**は文の途中で唐突に現れることが多い。
3. The quick brown fox jumps over the lazy dog near the river bank.

```sql
SELECT title FROM notes
WHERE trashed = 0;
```

> `toPlainText()` の結果がそのまま保存内容になる。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 328 | 395 |

## セクション 329

`toPlainText()` の結果がそのまま保存内容になる。

入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。タグは #work のように書き、#work/会議 で階層にする。

- 往復変換を挟まない設計は、データ破損を構造的に防ぐ。
- **強調**は文の途中で唐突に現れることが多い。
- タグは #work のように書き、#work/会議 で階層にする。

1. 書くという行為は、考えを外に出して初めて形が定まる。
2. エディタは書く速度を落としてはいけない。
3. 書くという行為は、考えを外に出して初めて形が定まる。

```python
def scan(text: str) -> list[InlineSpan]:
    return []
```

> The quick brown fox jumps over the lazy dog near the river bank.

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 335 | 720 |

## セクション 336

**強調**は文の途中で唐突に現れることが多い。

往復変換を挟まない設計は、データ破損を構造的に防ぐ。朝の光が机の上に落ちて、昨日の続きを促している。

- Rendering must stay incremental; a full rehighlight breaks the frame budget.
- The quick brown fox jumps over the lazy dog near the river bank.
- *斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。

1. The quick brown fox jumps over the lazy dog near the river bank.
2. タグは #work のように書き、#work/会議 で階層にする。
3. 往復変換を挟まない設計は、データ破損を構造的に防ぐ。

```sh
# コメント行
uv run pytest -q
```

> 書くという行為は、考えを外に出して初めて形が定まる。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 342 | 178 |

## セクション 343

`toPlainText()` の結果がそのまま保存内容になる。

*斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。`toPlainText()` の結果がそのまま保存内容になる。

- **強調**は文の途中で唐突に現れることが多い。
- 入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。
- 書くという行為は、考えを外に出して初めて形が定まる。

1. Rendering must stay incremental; a full rehighlight breaks the frame budget.
2. *斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。
3. 朝の光が机の上に落ちて、昨日の続きを促している。

```sh
# コメント行
uv run pytest -q
```

> **強調**は文の途中で唐突に現れることが多い。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 349 | 855 |

## セクション 350

The quick brown fox jumps over the lazy dog near the river bank.

**強調**は文の途中で唐突に現れることが多い。エディタは書く速度を落としてはいけない。

- Rendering must stay incremental; a full rehighlight breaks the frame budget.
- `toPlainText()` の結果がそのまま保存内容になる。
- 書くという行為は、考えを外に出して初めて形が定まる。

1. 入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。
2. 書くという行為は、考えを外に出して初めて形が定まる。
3. *斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。

```python
def scan(text: str) -> list[InlineSpan]:
    return []
```

> `toPlainText()` の結果がそのまま保存内容になる。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 356 | 643 |

## セクション 357

The quick brown fox jumps over the lazy dog near the river bank.

タグは #work のように書き、#work/会議 で階層にする。The quick brown fox jumps over the lazy dog near the river bank.

- The quick brown fox jumps over the lazy dog near the river bank.
- 書くという行為は、考えを外に出して初めて形が定まる。
- *斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。

1. 入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。
2. 書くという行為は、考えを外に出して初めて形が定まる。
3. エディタは書く速度を落としてはいけない。

```python
def scan(text: str) -> list[InlineSpan]:
    return []
```

> Rendering must stay incremental; a full rehighlight breaks the frame budget.

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 363 | 666 |

## セクション 364

The quick brown fox jumps over the lazy dog near the river bank.

*斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。設計上の判断はあとから理由を思い出せる形で残す。

- 設計上の判断はあとから理由を思い出せる形で残す。
- `toPlainText()` の結果がそのまま保存内容になる。
- `toPlainText()` の結果がそのまま保存内容になる。

1. *斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。
2. `toPlainText()` の結果がそのまま保存内容になる。
3. タグは #work のように書き、#work/会議 で階層にする。

```sh
# コメント行
uv run pytest -q
```

> 設計上の判断はあとから理由を思い出せる形で残す。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 370 | 338 |

## セクション 371

エディタは書く速度を落としてはいけない。

朝の光が机の上に落ちて、昨日の続きを促している。往復変換を挟まない設計は、データ破損を構造的に防ぐ。

- Rendering must stay incremental; a full rehighlight breaks the frame budget.
- 設計上の判断はあとから理由を思い出せる形で残す。
- エディタは書く速度を落としてはいけない。

1. エディタは書く速度を落としてはいけない。
2. *斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。
3. 朝の光が机の上に落ちて、昨日の続きを促している。

```sql
SELECT title FROM notes
WHERE trashed = 0;
```

> エディタは書く速度を落としてはいけない。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 377 | 545 |

## セクション 378

Rendering must stay incremental; a full rehighlight breaks the frame budget.

エディタは書く速度を落としてはいけない。朝の光が机の上に落ちて、昨日の続きを促している。

- Rendering must stay incremental; a full rehighlight breaks the frame budget.
- エディタは書く速度を落としてはいけない。
- 朝の光が机の上に落ちて、昨日の続きを促している。

1. タグは #work のように書き、#work/会議 で階層にする。
2. The quick brown fox jumps over the lazy dog near the river bank.
3. `toPlainText()` の結果がそのまま保存内容になる。

```sql
SELECT title FROM notes
WHERE trashed = 0;
```

> **強調**は文の途中で唐突に現れることが多い。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 384 | 485 |

## セクション 385

朝の光が机の上に落ちて、昨日の続きを促している。

エディタは書く速度を落としてはいけない。設計上の判断はあとから理由を思い出せる形で残す。

- エディタは書く速度を落としてはいけない。
- `toPlainText()` の結果がそのまま保存内容になる。
- 設計上の判断はあとから理由を思い出せる形で残す。

1. 往復変換を挟まない設計は、データ破損を構造的に防ぐ。
2. 設計上の判断はあとから理由を思い出せる形で残す。
3. The quick brown fox jumps over the lazy dog near the river bank.

```sh
# コメント行
uv run pytest -q
```

> Rendering must stay incremental; a full rehighlight breaks the frame budget.

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 391 | 749 |

## セクション 392

朝の光が机の上に落ちて、昨日の続きを促している。

`toPlainText()` の結果がそのまま保存内容になる。*斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。

- 朝の光が机の上に落ちて、昨日の続きを促している。
- The quick brown fox jumps over the lazy dog near the river bank.
- 入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。

1. 往復変換を挟まない設計は、データ破損を構造的に防ぐ。
2. 設計上の判断はあとから理由を思い出せる形で残す。
3. 朝の光が机の上に落ちて、昨日の続きを促している。

```python
def scan(text: str) -> list[InlineSpan]:
    return []
```

> 入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 398 | 444 |

## セクション 399

往復変換を挟まない設計は、データ破損を構造的に防ぐ。

*斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。`toPlainText()` の結果がそのまま保存内容になる。

- 設計上の判断はあとから理由を思い出せる形で残す。
- 朝の光が机の上に落ちて、昨日の続きを促している。
- The quick brown fox jumps over the lazy dog near the river bank.

1. *斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。
2. Rendering must stay incremental; a full rehighlight breaks the frame budget.
3. エディタは書く速度を落としてはいけない。

```python
def scan(text: str) -> list[InlineSpan]:
    return []
```

> タグは #work のように書き、#work/会議 で階層にする。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 405 | 984 |

## セクション 406

設計上の判断はあとから理由を思い出せる形で残す。

タグは #work のように書き、#work/会議 で階層にする。**強調**は文の途中で唐突に現れることが多い。

- 往復変換を挟まない設計は、データ破損を構造的に防ぐ。
- **強調**は文の途中で唐突に現れることが多い。
- 朝の光が机の上に落ちて、昨日の続きを促している。

1. 往復変換を挟まない設計は、データ破損を構造的に防ぐ。
2. エディタは書く速度を落としてはいけない。
3. *斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。

```sql
SELECT title FROM notes
WHERE trashed = 0;
```

> 設計上の判断はあとから理由を思い出せる形で残す。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 412 | 699 |

## セクション 413

`toPlainText()` の結果がそのまま保存内容になる。

朝の光が机の上に落ちて、昨日の続きを促している。The quick brown fox jumps over the lazy dog near the river bank.

- 朝の光が机の上に落ちて、昨日の続きを促している。
- `toPlainText()` の結果がそのまま保存内容になる。
- *斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。

1. 往復変換を挟まない設計は、データ破損を構造的に防ぐ。
2. **強調**は文の途中で唐突に現れることが多い。
3. *斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。

```sql
SELECT title FROM notes
WHERE trashed = 0;
```

> **強調**は文の途中で唐突に現れることが多い。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 419 | 799 |

## セクション 420

タグは #work のように書き、#work/会議 で階層にする。

`toPlainText()` の結果がそのまま保存内容になる。入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。

- タグは #work のように書き、#work/会議 で階層にする。
- 入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。
- 朝の光が机の上に落ちて、昨日の続きを促している。

1. タグは #work のように書き、#work/会議 で階層にする。
2. 朝の光が机の上に落ちて、昨日の続きを促している。
3. 設計上の判断はあとから理由を思い出せる形で残す。

```python
def scan(text: str) -> list[InlineSpan]:
    return []
```

> 設計上の判断はあとから理由を思い出せる形で残す。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 426 | 610 |

## セクション 427

Rendering must stay incremental; a full rehighlight breaks the frame budget.

タグは #work のように書き、#work/会議 で階層にする。入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。

- 入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。
- タグは #work のように書き、#work/会議 で階層にする。
- Rendering must stay incremental; a full rehighlight breaks the frame budget.

1. エディタは書く速度を落としてはいけない。
2. *斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。
3. タグは #work のように書き、#work/会議 で階層にする。

```sh
# コメント行
uv run pytest -q
```

> タグは #work のように書き、#work/会議 で階層にする。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 433 | 364 |

## セクション 434

Rendering must stay incremental; a full rehighlight breaks the frame budget.

書くという行為は、考えを外に出して初めて形が定まる。入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。

- 書くという行為は、考えを外に出して初めて形が定まる。
- `toPlainText()` の結果がそのまま保存内容になる。
- **強調**は文の途中で唐突に現れることが多い。

1. `toPlainText()` の結果がそのまま保存内容になる。
2. タグは #work のように書き、#work/会議 で階層にする。
3. **強調**は文の途中で唐突に現れることが多い。

```sh
# コメント行
uv run pytest -q
```

> Rendering must stay incremental; a full rehighlight breaks the frame budget.

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 440 | 353 |

## セクション 441

入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。

*斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。エディタは書く速度を落としてはいけない。

- 設計上の判断はあとから理由を思い出せる形で残す。
- **強調**は文の途中で唐突に現れることが多い。
- *斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。

1. *斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。
2. エディタは書く速度を落としてはいけない。
3. タグは #work のように書き、#work/会議 で階層にする。

```sql
SELECT title FROM notes
WHERE trashed = 0;
```

> 往復変換を挟まない設計は、データ破損を構造的に防ぐ。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 447 | 567 |

## セクション 448

*斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。

Rendering must stay incremental; a full rehighlight breaks the frame budget.朝の光が机の上に落ちて、昨日の続きを促している。

- **強調**は文の途中で唐突に現れることが多い。
- 入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。
- エディタは書く速度を落としてはいけない。

1. エディタは書く速度を落としてはいけない。
2. `toPlainText()` の結果がそのまま保存内容になる。
3. Rendering must stay incremental; a full rehighlight breaks the frame budget.

```python
def scan(text: str) -> list[InlineSpan]:
    return []
```

> `toPlainText()` の結果がそのまま保存内容になる。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 454 | 377 |

## セクション 455

設計上の判断はあとから理由を思い出せる形で残す。

朝の光が机の上に落ちて、昨日の続きを促している。*斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。

- 書くという行為は、考えを外に出して初めて形が定まる。
- *斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。
- 入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。

1. Rendering must stay incremental; a full rehighlight breaks the frame budget.
2. The quick brown fox jumps over the lazy dog near the river bank.
3. 書くという行為は、考えを外に出して初めて形が定まる。

```sh
# コメント行
uv run pytest -q
```

> 往復変換を挟まない設計は、データ破損を構造的に防ぐ。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 461 | 830 |

## セクション 462

タグは #work のように書き、#work/会議 で階層にする。

The quick brown fox jumps over the lazy dog near the river bank.**強調**は文の途中で唐突に現れることが多い。

- `toPlainText()` の結果がそのまま保存内容になる。
- 朝の光が机の上に落ちて、昨日の続きを促している。
- 往復変換を挟まない設計は、データ破損を構造的に防ぐ。

1. 設計上の判断はあとから理由を思い出せる形で残す。
2. タグは #work のように書き、#work/会議 で階層にする。
3. `toPlainText()` の結果がそのまま保存内容になる。

```sh
# コメント行
uv run pytest -q
```

> 往復変換を挟まない設計は、データ破損を構造的に防ぐ。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 468 | 993 |

## セクション 469

The quick brown fox jumps over the lazy dog near the river bank.

設計上の判断はあとから理由を思い出せる形で残す。書くという行為は、考えを外に出して初めて形が定まる。

- The quick brown fox jumps over the lazy dog near the river bank.
- 往復変換を挟まない設計は、データ破損を構造的に防ぐ。
- 往復変換を挟まない設計は、データ破損を構造的に防ぐ。

1. 入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。
2. 往復変換を挟まない設計は、データ破損を構造的に防ぐ。
3. The quick brown fox jumps over the lazy dog near the river bank.

```sh
# コメント行
uv run pytest -q
```

> タグは #work のように書き、#work/会議 で階層にする。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 475 | 76 |

## セクション 476

入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。

往復変換を挟まない設計は、データ破損を構造的に防ぐ。*斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。

- Rendering must stay incremental; a full rehighlight breaks the frame budget.
- 入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。
- 設計上の判断はあとから理由を思い出せる形で残す。

1. Rendering must stay incremental; a full rehighlight breaks the frame budget.
2. 入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。
3. 書くという行為は、考えを外に出して初めて形が定まる。

```sh
# コメント行
uv run pytest -q
```

> Rendering must stay incremental; a full rehighlight breaks the frame budget.

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 482 | 818 |

## セクション 483

タグは #work のように書き、#work/会議 で階層にする。

Rendering must stay incremental; a full rehighlight breaks the frame budget.*斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。

- *斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。
- `toPlainText()` の結果がそのまま保存内容になる。
- The quick brown fox jumps over the lazy dog near the river bank.

1. Rendering must stay incremental; a full rehighlight breaks the frame budget.
2. Rendering must stay incremental; a full rehighlight breaks the frame budget.
3. タグは #work のように書き、#work/会議 で階層にする。

```sh
# コメント行
uv run pytest -q
```

> 入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 489 | 540 |

## セクション 490

往復変換を挟まない設計は、データ破損を構造的に防ぐ。

`toPlainText()` の結果がそのまま保存内容になる。入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。

- 設計上の判断はあとから理由を思い出せる形で残す。
- The quick brown fox jumps over the lazy dog near the river bank.
- 朝の光が机の上に落ちて、昨日の続きを促している。

1. The quick brown fox jumps over the lazy dog near the river bank.
2. The quick brown fox jumps over the lazy dog near the river bank.
3. エディタは書く速度を落としてはいけない。

```python
def scan(text: str) -> list[InlineSpan]:
    return []
```

> 設計上の判断はあとから理由を思い出せる形で残す。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 496 | 752 |

## セクション 497

*斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。

The quick brown fox jumps over the lazy dog near the river bank.タグは #work のように書き、#work/会議 で階層にする。

- The quick brown fox jumps over the lazy dog near the river bank.
- 朝の光が机の上に落ちて、昨日の続きを促している。
- 入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。

1. 往復変換を挟まない設計は、データ破損を構造的に防ぐ。
2. The quick brown fox jumps over the lazy dog near the river bank.
3. 往復変換を挟まない設計は、データ破損を構造的に防ぐ。

```sql
SELECT title FROM notes
WHERE trashed = 0;
```

> *斜体*と~~取り消し~~を同じ段落に混ぜても破綻しないこと。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 503 | 657 |

## セクション 504

書くという行為は、考えを外に出して初めて形が定まる。

書くという行為は、考えを外に出して初めて形が定まる。**強調**は文の途中で唐突に現れることが多い。

- タグは #work のように書き、#work/会議 で階層にする。
- 設計上の判断はあとから理由を思い出せる形で残す。
- タグは #work のように書き、#work/会議 で階層にする。

1. エディタは書く速度を落としてはいけない。
2. エディタは書く速度を落としてはいけない。
3. 朝の光が机の上に落ちて、昨日の続きを促している。

```python
def scan(text: str) -> list[InlineSpan]:
    return []
```

> タグは #work のように書き、#work/会議 で階層にする。

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 510 | 220 |

## セクション 511

書くという行為は、考えを外に出して初めて形が定まる。

朝の光が机の上に落ちて、昨日の続きを促している。設計上の判断はあとから理由を思い出せる形で残す。

- `toPlainText()` の結果がそのまま保存内容になる。
- 書くという行為は、考えを外に出して初めて形が定まる。
- タグは #work のように書き、#work/会議 で階層にする。

1. `toPlainText()` の結果がそのまま保存内容になる。
2. 入力遅延が 16ms を超えると、指の動きと画面がずれて感じられる。
3. Rendering must stay incremental; a full rehighlight breaks the frame budget.

```python
def scan(text: str) -> list[InlineSpan]:
    return []
```

> Rendering must stay incremental; a full rehighlight breaks the frame budget.

- [ ] やること
- [x] 済み

| 項目 | 値 |
|---|---|
| 行 517 | 726 |

## セクション 518

書くという行為は、考えを外に出して初めて形が定まる。

朝の光が机の上に落ちて、昨日の続きを促している。往復変換を挟まない設計は、データ破損を構造的に防ぐ。

